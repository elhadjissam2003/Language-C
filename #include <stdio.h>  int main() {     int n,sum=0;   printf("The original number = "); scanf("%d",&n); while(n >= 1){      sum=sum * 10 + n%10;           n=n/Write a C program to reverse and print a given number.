 #include <stdio.h>

int main() {
    int n,sum=0;


printf("The original number = ");
scanf("%d",&n);
while(n >= 1){
     sum=sum * 10 + n%10;
     
    n=n/10;
}

printf("\nThe reverse of the said number = %d",sum);


    return 0;
}
